[server]
headless = true
# Optimizations for Streamlit Cloud
maxUploadSize = 200  # MB - limit upload size
maxMessageSize = 200  # MB

[browser]
gatherUsageStats = false

[theme]
primaryColor = "#FF6B6B"
backgroundColor = "#FFFFFF"
secondaryBackgroundColor = "#F0F2F6"
textColor = "#262730"
font = "sans serif"
